import { suite, test } from 'mocha';
import * as sinon from 'sinon';
import * as chai from 'chai';

suite("JDK Downloader", () => {
suite("JDK Downloader Part-1", () => {
        test("Sum 1 and 2", () => {
            chai.expect((1 + 2) === 3, "Sum not equals");
        });
    });
});